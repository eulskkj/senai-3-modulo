import random
import os
os.system("cls")

numeros = [23, 44, 56, 74, 1, 21, 33 ]
nome = ['Luis', 'Henrique', 'Ls' ]

#-----------------------------------------------
#append: add o elemento ao ultimo andar.
#insert: add na posição que eu quiser.

#-----------------------------------------------
#pop: remove e retorna o ultimo elemento.
#remove: remove a primeira ocorrência do valor. 

#-----------------------------------------------
#sort: Ordena o vetor.
#reverse: Reversa o vetor.
#len: quantos valores tem no vetor.
#max: retorna qual é o maior valor encontrado em um ventor.
#min: retorna qual é o menor valor encontrado em um ventor.
#sum: soma o que armazenou no vetor.
#random.choice: Sortear aleatoriamente um valor do vetor.

sortudo = random.choice(nome)

print(f"O sortudo é {sortudo}")